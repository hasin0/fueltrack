apiVersion: v1
kind: Service
metadata:
  name: {{ include "fueltrack.fullname" . }}
  labels:
    {{- include "fueltrack.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fueltrack.selectorLabels" . | nindent 4 }}
